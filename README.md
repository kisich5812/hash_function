# Простая Hash функция

Консольная программа, реализующая простой алгоритм хеширования введённой текстовой фразы. Позволяет генерировать хеш заданной длины с использованием заданного алфавита.

---

## Возможности
* **Настройка длины**: создание хеша любой нужной длинны.
* **Гибкий алфавит**: возможность ограничить символы хеша(например, только цифры).

---

## 🛠 Параметры запуска

| Флаг | Аргумент | Описание |
| :--- | :--- | :--- |
| `--len` | `<число>` | Определяет итоговую длину хеш-строки. |
| `--alph` | `<строка>` | Набор символов, из которых будет состоять хеш. |
| `--help` | — | Выводит краткую справку по использованию. |

---

## Сборка

```bash
git clone https://github.com/kisich5812/hash_function.git
cd hash_function && make
```

---

## Примеры использования

**Запуск программы для создания хеша длиной 16 знаков из набора «abcd123»:**
```bash
./hash --len 16 --alph "abcd123"
```
